import 'package:flutter/material.dart';

class AirPlayScreen extends StatelessWidget {
  const AirPlayScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return AppBar(title: Text("AirPlay"),);
  }
}
